//  promise 

// ? pending
// ? success
// ? failure

//  fetch data from fb api
const getsum = (x, y, callback) =>{
    setTimeout(() =>{
        let sum = x +y;
        let error =false;
    })
}
